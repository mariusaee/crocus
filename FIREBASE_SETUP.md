# 🔥 Инструкция по настройке Firebase

Для синхронизации цветов между устройствами нужно настроить Firebase Realtime Database.

## Шаг 1: Создание проекта Firebase

1. Перейдите на https://console.firebase.google.com
2. Нажмите "Добавить проект" (Add project)
3. Введите название проекта: `crocus-garden` (или любое другое)
4. Отключите Google Analytics (не нужен для этого проекта)
5. Нажмите "Создать проект"

## Шаг 2: Создание Realtime Database

1. В левом меню выберите "Build" → "Realtime Database"
2. Нажмите "Создать базу данных" (Create Database)
3. Выберите регион: `europe-west1` (Бельгия) - ближайший к вам
4. **ВАЖНО:** Выберите режим "Начать в тестовом режиме" (Start in test mode)
5. Нажмите "Включить"

## Шаг 3: Настройка правил безопасности

После создания базы данных:

1. Перейдите во вкладку "Правила" (Rules)
2. Замените правила на следующие:

```json
{
  "rules": {
    "flowers": {
      ".read": true,
      ".write": true
    }
  }
}
```

3. Нажмите "Опубликовать" (Publish)

**Примечание:** Эти правила позволяют всем читать и записывать данные. Для production рекомендуется добавить аутентификацию.

## Шаг 4: Получение конфигурации

1. Нажмите на иконку шестерёнки ⚙️ рядом с "Обзор проекта"
2. Выберите "Настройки проекта" (Project settings)
3. Прокрутите вниз до раздела "Ваши приложения" (Your apps)
4. Нажмите на иконку `</>` (Web)
5. Зарегистрируйте приложение:
   - Псевдоним: `crocus-web`
   - НЕ отмечайте "Firebase Hosting"
   - Нажмите "Зарегистрировать приложение"
6. Скопируйте объект `firebaseConfig`

## Шаг 5: Добавление конфигурации в проект

1. Откройте файл `src/firebase.js`
2. Замените `firebaseConfig` на ваш:

```javascript
const firebaseConfig = {
  apiKey: "ВАШ_API_KEY",
  authDomain: "ваш-проект.firebaseapp.com",
  databaseURL: "https://ваш-проект-default-rtdb.europe-west1.firebasedatabase.app",
  projectId: "ваш-проект",
  storageBucket: "ваш-проект.appspot.com",
  messagingSenderId: "1234567890",
  appId: "1:1234567890:web:abcdef123456"
}
```

3. Сохраните файл
4. Сделайте commit и deploy:
```bash
git add src/firebase.js
git commit -m "Добавлена конфигурация Firebase"
git push
npm run deploy
```

## ✅ Готово!

После деплоя:
- Откройте сайт на телефоне: https://mariusaee.github.io/crocus/add
- Добавьте цветок
- Откройте сайт на компьютере: https://mariusaee.github.io/crocus
- Цветок появится автоматически! 🌸

## 🔄 Как это работает

- **Realtime Database** хранит все цветы в облаке
- При добавлении цветка → сохраняется в Firebase
- Firebase мгновенно уведомляет все открытые вкладки
- Все устройства видят изменения в реальном времени

## 🛡️ Безопасность (опционально)

Для продакшена рекомендуется:
1. Добавить Firebase Authentication
2. Обновить правила доступа
3. Ограничить запросы по IP или домену

## 🆘 Проблемы?

Если что-то не работает:
1. Проверьте консоль браузера (F12)
2. Убедитесь, что правильно скопировали `firebaseConfig`
3. Проверьте правила безопасности в Firebase Console
4. Убедитесь, что `databaseURL` содержит правильный регион
